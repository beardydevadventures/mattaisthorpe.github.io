<div class="pill"><slot /></div>

<style>
	.pill {
		display: flex;
		align-items: center;
		padding: 0.5rem 1rem;
		gap: 0.5rem;
		color: var(--gray-100);
		border: 1px solid color-mix(in srgb, var(--accent-dark), transparent 68%);
		background: var(--surface-menu);
		border-radius: 999rem;
		font-size: var(--text-md);
		line-height: 1.35;
		white-space: nowrap;
		box-shadow: 0 10px 24px rgba(9, 11, 17, 0.08);
	}

	.pill :global(svg) {
		color: var(--accent-dark);
	}
</style>
