---
import { getCollection } from "astro:content";

// Layout import — provides basic page elements: <head>, <nav>, <footer> etc.
import BaseLayout from "../layouts/BaseLayout.astro";

// Component Imports
import CallToAction from "../components/CallToAction.astro";
import Grid from "../components/Grid.astro";
import Hero from "../components/Hero.astro";
import Icon from "../components/Icon.astro";
import Pill from "../components/Pill.astro";
import PortfolioPreview from "../components/PortfolioPreview.astro";
import PostPreview from "../components/PostPreview.astro";

// Page section components
import ContactCTA from "../components/ContactCTA.astro";
import Skills from "../components/Skills.astro";

// Content Fetching: List four most recent blog posts
const posts = (await getCollection("posts"))
	.sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf())
	.slice(0, 4);

// Content Fetching: List four most recent work projects
const projects = (await getCollection("projects"))
	.filter((p) => p.data.featured) // Only keep featured projects
	.sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf()) // Sort by date
	.slice(0, 4); // Show only 4

// Full Astro Component Syntax:
// https://docs.astro.build/basics/astro-components/
---

<BaseLayout>
	<div class="stack gap-14 lg:gap-28">
		<div class="wrapper stack gap-8 lg:gap-16">
			<header class="hero">
				<Hero
					title="Hey there, I'm Matthew Aisthorpe"
					tagline="I lead technical teams delivering AI-enabled platforms, XR experiences, and modern digital systems from strategy through production."
					align="start"
				>
					<div class="roles">
						<Pill
							><Icon icon="strategy" size="1.33em" />Delivery
							Leader</Pill
						>
						<Pill
							><Icon icon="code" size="1.33em" />Product &
							Platform Builder</Pill
						>
						<Pill
							><Icon icon="users-three" size="1.33em" />XR & AI
							Practitioner</Pill
						>
					</div>
				</Hero>

				<img
					alt="Matthew Aisthorpe"
					width="480"
					height="620"
					src="/assets/portrait.jpg"
				/>
			</header>

			<Skills />

			<section class="section with-cta !gap-0">
				<div class="surface card-block stack gap-4 lg:gap-6">
					<header class="stack gap-2 lg:gap-4">
						<h2>Professional Career</h2>
						<p>
							Want a deeper look at my work in Agile leadership,
							digital transformation, and immersive tech?
						</p>
					</header>

					<div class="cta career-cta mt-2 lg:mt-0">
						<CallToAction
							href="https://www.linkedin.com/in/matthewaisthorpe/"
						>
							View My LinkedIn
							<Icon icon="arrow-right" size="1.1em" />
						</CallToAction>
					</div>
				</div>
			</section>
		</div>

		<main class="wrapper stack gap-16 lg:gap-24">
			<section class="section with-background with-cta centered">
				<header class="section-header stack gap-2 lg:gap-4">
					<h2>Blog Posts</h2>
					<p>
						Check out my most recent blog posts, where I share
						insights, experiences, and ideas.
					</p>
				</header>

				<div class="gallery">
					<Grid variant="offset">
						{
							posts.map((post) => (
								<li>
									<PostPreview post={post} />
								</li>
							))
						}
					</Grid>
				</div>

				<div class="cta">
					<CallToAction href="/blog/">
						View Blog
						<Icon icon="arrow-right" size="1.2em" />
					</CallToAction>
				</div>
			</section>

			<section class="section with-background with-cta centered">
				<header class="section-header stack gap-2 lg:gap-4">
					<h2>Personal Project Highlights</h2>
					<p>
						A selection of projects showcasing delivery across AI, XR, and modern web systems.
					</p>
				</header>

				<div class="gallery">
					<Grid variant="offset">
						{
							projects.map((project) => (
								<li>
									<PortfolioPreview project={project} />
								</li>
							))
						}
					</Grid>
				</div>

				<div class="cta">
					<CallToAction href="/projects/">
						View All Projects
						<Icon icon="arrow-right" size="1.2em" />
					</CallToAction>
				</div>
			</section>

			<section class="section with-background compact">
				<header class="section-header stack gap-2 lg:gap-4">
					<h2>Creative Brands</h2>
					<p>
						My creative brands where I explore immersive tech and
						share dev insights.
					</p>
				</header>

				<div class="gallery creative-brands">
					<Grid variant="offset">
						<li>
							<img
								src="/assets/fovr-logo.png"
								alt="FoVR Interactive logo"
								class="h-24 object-contain mb-6 mx-auto"
							/>
							<h3 class="text-2xl font-semibold mb-2">
								FoVR Interactive
							</h3>
							<p class="text-gray-400 mb-6">
								My experimental playground for immersive
								experiences in VR, XR, and spatial computing.
								Projects like
								<em>Forge of Elements</em> and <em
									>Smash Room</em
								> explore new ways we interact, play, and build in
								virtual spaces.
							</p>
							<CallToAction href="https://fovrinteractive.net/">
								Visit FoVR Interactive
								<Icon icon="arrow-right" size="1.1em" />
							</CallToAction>
						</li>

						<li>
							<img
								src="/assets/beardydev-logo.jpg"
								alt="Beardy Dev Adventures logo"
								class="w-24 h-24 object-cover rounded-full mb-6 mx-auto"
							/>
							<h3 class="text-2xl font-semibold mb-2">
								Beardy Dev Adventures
							</h3>
							<p class="text-gray-400 mb-6">
								A devlog and YouTube series where I share dev
								experiments, immersive gameplay, and lessons
								from my experience working in the digital
								industry.
							</p>
							<CallToAction
								href="https://www.youtube.com/@beardydevadventures"
							>
								Explore Beardy Dev Adventures
								<Icon icon="arrow-right" size="1.1em" />
							</CallToAction>
						</li>
					</Grid>
				</div>
			</section>

			<section class="section with-background compact">
				<header class="section-header stack gap-2 lg:gap-4">
					<h2>Programs & Recognition</h2>
					<p>
						I've been fortunate to take part in a range of programs
						and platforms supporting immersive technology, digital
						innovation, and creative development.
					</p>
				</header>

				<div class="gallery">
					<Grid variant="small">
						{
							[
								"Meta Horizon Start Program - Start Partner",
								"Multi-Platform App Releases (Meta, Android, iOS, Steam)",
								"Meta Horizon Creator Program",
								"Meta Connect 2025 - Invited Start Partner",
								"GovHack Winner (2019, 2022) & Runner-Up (2021)",
								"QLD XR Hub Member",
							].map((brand) => (
								<li class="mention-card">
									<p>{brand}</p>
								</li>
							))
						}
					</Grid>
				</div>
			</section>
		</main>

		<ContactCTA />
	</div>
</BaseLayout>

<style>
	.hero {
		position: relative;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 2.5rem;
		padding: 1.25rem;
		border: 1px solid color-mix(in srgb, var(--gray-800), transparent 18%);
		border-radius: 1.5rem;
		background:
			radial-gradient(circle at 20% 12%, rgba(35, 199, 217, 0.2), transparent 31%),
			radial-gradient(circle at 82% 24%, rgba(255, 140, 107, 0.16), transparent 29%),
			linear-gradient(135deg, hsla(var(--gray-999-basis), 0.7), hsla(var(--gray-999-basis), 0.34));
		box-shadow: var(--shadow-premium);
		overflow: hidden;
		backdrop-filter: blur(18px);
	}

	.hero::before {
		content: "";
		position: absolute;
		inset: auto -8rem -11rem auto;
		width: 24rem;
		aspect-ratio: 1;
		border-radius: 999rem;
		background: conic-gradient(from 160deg, rgba(35, 199, 217, 0.2), rgba(197, 97, 246, 0.24), rgba(255, 140, 107, 0.18), rgba(35, 199, 217, 0.2));
		filter: blur(10px);
		opacity: 0.72;
		pointer-events: none;
	}

	.hero > :global(.hero),
	.hero img {
		position: relative;
		z-index: 1;
	}

	.roles {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: 0.65rem;
		margin-top: 0.75rem;
	}

	.hero img {
		width: min(100%, 28rem);
		aspect-ratio: 5 / 4;
		object-fit: cover;
		object-position: top;
		border-radius: 1.25rem;
		border: 1px solid hsla(var(--gray-999-basis), 0.52);
		box-shadow: 0 28px 70px rgba(9, 11, 17, 0.18);
		filter: saturate(1.04) contrast(1.03);
	}

	.section-header {
		font-size: var(--text-xl);
		text-align: center;
	}

	@media (min-width: 50em) {
		.hero {
			display: grid;
			grid-template-columns: minmax(0, 1.08fr) minmax(18rem, 0.82fr);
			padding: clamp(2rem, 4vw, 3.5rem);
			gap: clamp(2.5rem, 6vw, 5rem);
		}

		.hero img {
			aspect-ratio: 3 / 4;
			border-radius: 2rem;
			object-fit: cover;
			justify-self: end;
		}

		h2 {
			font-size: var(--text-3xl);
		}
	}

	/* ====================================================== */

	.section {
		display: grid;
		gap: 2.5rem;
		position: relative;
	}

	.section.centered .section-header {
		justify-self: center;
		text-align: center;
	}

	.section.compact {
		gap: 1.25rem;
	}

	.section.with-cta {
		display: block;
	}

	.surface {
		position: relative;
		background:
			linear-gradient(135deg, hsla(var(--gray-999-basis), 0.76), hsla(var(--gray-999-basis), 0.4)),
			var(--gradient-premium);
		border: 1px solid color-mix(in srgb, var(--gray-800), transparent 18%);
		border-radius: 1.25rem;
		padding: 1.5rem;
		box-shadow: var(--shadow-premium);
		overflow: hidden;
		backdrop-filter: blur(18px);
	}

	.surface::before {
		content: "";
		position: absolute;
		inset: 0;
		pointer-events: none;
		background: radial-gradient(circle at 94% 20%, rgba(35, 199, 217, 0.16), transparent 28%);
	}

	.card-block {
		max-width: 100%;
		margin-inline: 0;
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.cta {
		display: flex;
		justify-content: center;
	}

	.career-cta {
		margin-left: auto;
	}

	.career-cta :global(a) {
		padding: 0.65rem 1.25rem;
		gap: 0.5rem;
		font-size: var(--text-md);
	}

	.with-background {
		position: relative;
	}

	.with-background::before {
		--hero-bg: var(--bg-image-subtle-2);

		content: "";
		position: absolute;
		pointer-events: none;
		left: 50%;
		width: 100vw;
		aspect-ratio: calc(2.25 / var(--bg-scale));
		top: 0;
		transform: translateY(-75%) translateX(-50%);
		background:
			url("/assets/backgrounds/noise.png") top center/220px repeat,
			var(--hero-bg) center center / var(--bg-gradient-size) no-repeat,
			var(--gray-999);
		background-blend-mode: overlay, normal, normal, normal;
		mix-blend-mode: var(--bg-blend-mode);
		z-index: -2;
		opacity: 0.72;
	}

	.with-background.bg-variant::before {
		--hero-bg: var(--bg-image-subtle-1);
	}

	.section-header {
		justify-self: center;
		text-align: center;
		max-width: 70ch;
		font-size: var(--text-md);
		color: var(--gray-300);
		text-wrap: pretty;
	}

	.creative-brands p {
		max-width: 50ch;
	}

	.creative-brands li {
		position: relative;
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		gap: 0.75rem;
		padding: 1.5rem;
		border: 1px solid var(--gray-800);
		border-radius: 1.25rem;
		background:
			linear-gradient(135deg, hsla(var(--gray-999-basis), 0.72), hsla(var(--gray-999-basis), 0.34)),
			var(--gradient-subtle);
		box-shadow: var(--shadow-sm);
		overflow: hidden;
		transition:
			transform var(--theme-transition),
			box-shadow var(--theme-transition),
			border-color var(--theme-transition);
	}

	.creative-brands li:hover {
		transform: translateY(-4px);
		border-color: color-mix(in srgb, var(--accent-dark), var(--gray-800) 58%);
		box-shadow: var(--shadow-premium);
	}

	.section.with-background .gallery {
		margin-top: 2rem;
		margin-bottom: 2rem;
	}

	.section.compact .section-header {
		gap: 0.5rem;
	}

	.section.compact .gallery {
		margin-top: 1rem;
		margin-bottom: 1rem;
	}

	.section-header h3,
	.section-header h2 {
		font-size: var(--text-2xl);
		text-wrap: balance;
	}

	.section.with-cta .section-header h2 {
		font-size: var(--text-2xl);
	}

	.card-block header h2 {
		font-size: var(--text-xl);
	}

	@media (min-width: 50em) {
		.section {
			grid-template-columns: repeat(4, 1fr);
			grid-template-areas: "header header header header" "gallery gallery gallery gallery";
			gap: 4.5rem;
		}

		.section.with-cta {
			grid-template-areas: "header header header cta" "gallery gallery gallery gallery";
		}

		.section-header {
			grid-area: header;
			font-size: var(--text-lg);
		}

		.creative-brands p {
			max-width: 100%;
		}

		.section-header h3,
		.section-header h2 {
			font-size: var(--text-4xl);
		}

		.section.with-cta .section-header h2 {
			font-size: var(--text-4xl);
		}

		.card-block header h2 {
			font-size: var(--text-3xl);
		}

		.section:not(.with-cta) .section-header {
			text-align: center;
		}

		.with-cta .section-header {
			justify-self: flex-start;
			text-align: left;
		}

		.section.centered.with-cta .section-header {
			justify-self: center;
			text-align: center;
		}

		.card-block {
			flex-direction: row;
			align-items: center;
			column-gap: 2rem;
			padding: 2rem;
		}

		.card-block header {
			flex: 1;
		}

		.career-cta {
			justify-content: flex-end;
		}

		.gallery {
			grid-area: gallery;
		}

		.cta {
			grid-area: cta;
		}
	}

	/* ====================================================== */

	.mention-card {
		position: relative;
		display: flex;
		height: 7rem;
		justify-content: center;
		align-items: center;
		text-align: center;
		border: 1px solid var(--gray-800);
		border-radius: 1rem;
		color: var(--gray-300);
		background:
			linear-gradient(135deg, hsla(var(--gray-999-basis), 0.68), hsla(var(--gray-999-basis), 0.3)),
			var(--gradient-premium);
		box-shadow: var(--shadow-sm);
		padding: 1rem;
		overflow: hidden;
		transition:
			transform var(--theme-transition),
			border-color var(--theme-transition),
			box-shadow var(--theme-transition);
	}

	.mention-card::before {
		content: "";
		position: absolute;
		inset: 0;
		background: radial-gradient(circle at 50% 0%, rgba(35, 199, 217, 0.12), transparent 42%);
		opacity: 0;
		transition: opacity var(--theme-transition);
	}

	.mention-card:hover {
		transform: translateY(-3px);
		border-color: color-mix(in srgb, var(--accent-dark), var(--gray-800) 62%);
		box-shadow: var(--shadow-md);
	}

	.mention-card:hover::before {
		opacity: 1;
	}

	.mention-card p {
		position: relative;
		z-index: 1;
	}

	@media (min-width: 50em) {
		.mention-card {
			border-radius: 1.5rem;
			height: 9.5rem;
		}
	}

	.creative-brands :global(a) {
		padding: 0.65rem 1.25rem;
		gap: 0.5rem;
		font-size: var(--text-md);
		width: fit-content;
		align-self: center;
	}
</style>
